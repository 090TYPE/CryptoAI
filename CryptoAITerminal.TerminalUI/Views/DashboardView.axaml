<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vmd="clr-namespace:CryptoAITerminal.TerminalUI.ViewModels.Dashboard"
             xmlns:ctrl="clr-namespace:CryptoAITerminal.TerminalUI.Controls"
             xmlns:dash="clr-namespace:CryptoAITerminal.TerminalUI.Views.Dashboard"
             x:Class="CryptoAITerminal.TerminalUI.Views.DashboardView"
             x:DataType="vmd:DashboardLayoutViewModel"
             x:CompileBindings="True">
  <DockPanel LastChildFill="True">
    <Grid DockPanel.Dock="Top" ColumnDefinitions="*,Auto,Auto,Auto,Auto" Margin="0,0,0,16">
      <StackPanel Grid.Column="0" Spacing="4" VerticalAlignment="Center">
        <TextBlock Classes="H1" Text="Dashboard" />
        <TextBlock Classes="StatLabel"
                   Text="{Binding Widgets.Count, StringFormat='DEFAULT LAYOUT · {0} WIDGETS'}" />
      </StackPanel>

      <!-- LIVE indicator -->
      <Border Grid.Column="1" Classes="LivePill" Margin="0,0,10,0" VerticalAlignment="Center">
        <StackPanel Orientation="Horizontal" Spacing="6" VerticalAlignment="Center">
          <Ellipse Width="7" Height="7" Fill="{DynamicResource Accent}" VerticalAlignment="Center" />
          <TextBlock Text="LIVE" Foreground="{DynamicResource Accent}" FontSize="{DynamicResource FsCaption}" FontWeight="Bold" LetterSpacing="0.8" />
        </StackPanel>
      </Border>

      <Button Grid.Column="2" Content="+ Widget" Margin="0,0,8,0" VerticalAlignment="Center">
        <Button.Flyout>
          <Flyout Placement="Bottom">
            <ScrollViewer MaxHeight="360">
              <ItemsControl ItemsSource="{Binding AddableCatalog}" Width="240">
                <ItemsControl.ItemTemplate>
                  <DataTemplate x:DataType="vmd:WidgetCatalogEntry">
                    <Button Content="{Binding Title}"
                            HorizontalAlignment="Stretch"
                            HorizontalContentAlignment="Left"
                            Margin="0,0,0,4"
                            Command="{Binding $parent[ItemsControl].((vmd:DashboardLayoutViewModel)DataContext).AddWidgetCommand}"
                            CommandParameter="{Binding Key}" />
                  </DataTemplate>
                </ItemsControl.ItemTemplate>
              </ItemsControl>
            </ScrollViewer>
          </Flyout>
        </Button.Flyout>
      </Button>
      <Button Grid.Column="3" Content="↺ Reset" Margin="0,0,8,0" VerticalAlignment="Center"
              IsVisible="{Binding IsEditMode}"
              Command="{Binding ResetLayoutCommand}" />
      <Button Grid.Column="4" Content="✎ Edit Layout" Classes="AccentButton" VerticalAlignment="Center"
              Command="{Binding ToggleEditCommand}" />
    </Grid>

    <ScrollViewer Classes="PageScroll">
      <ItemsControl ItemsSource="{Binding Widgets}">
        <ItemsControl.ItemsPanel>
          <ItemsPanelTemplate>
            <ctrl:WidgetGridPanel />
          </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
        <ItemsControl.Styles>
          <!-- The setters bind the ContentPresenter's item (DashboardWidget), not this view's
               DataContext, and a Style carries no x:DataType — a compiled binding would resolve
               Col/Row against DashboardLayoutViewModel and fail. Reflection binding per setter,
               so the rest of the file stays compiled. -->
          <Style Selector="ContentPresenter">
            <Setter Property="(ctrl:WidgetGridPanel.Col)"     Value="{ReflectionBinding Col}" />
            <Setter Property="(ctrl:WidgetGridPanel.Row)"     Value="{ReflectionBinding Row}" />
            <Setter Property="(ctrl:WidgetGridPanel.ColSpan)" Value="{ReflectionBinding ColSpan}" />
            <Setter Property="(ctrl:WidgetGridPanel.RowSpan)" Value="{ReflectionBinding RowSpan}" />
          </Style>
        </ItemsControl.Styles>
        <ItemsControl.ItemTemplate>
          <DataTemplate x:DataType="vmd:DashboardWidget">
            <dash:WidgetHost IsEditMode="{Binding $parent[ItemsControl].((vmd:DashboardLayoutViewModel)DataContext).IsEditMode}" />
          </DataTemplate>
        </ItemsControl.ItemTemplate>
      </ItemsControl>
    </ScrollViewer>
  </DockPanel>
</UserControl>
